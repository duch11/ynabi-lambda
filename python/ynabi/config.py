#download_path="."
id_postfix = ""  # Postfix for filtering duplicate transactions
start_date = "2025-04-20T00:00:00Z"  # Date of earliest transactions to import to YNAB

#from datetime import datetime, timedelta
#yesterday = datetime.utcnow() - timedelta(days=1)
#start_date = yesterday.strftime("%Y-%m-%dT00:00:00Z")
